platform: linux

image_resource:
  type: docker-image
  source:
    repository: jchesterpivotal/concourse-build-resource
    tag: v0.6.0-rc.14

inputs:
- name: build

run:
  path: /opt/tasks/show-resources
